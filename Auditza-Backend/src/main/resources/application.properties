spring.application.name=Auditza-Backend
# Configuration PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/auditej_db
spring.datasource.username=postgres
spring.datasource.password=Builder3498
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuration JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Configuration JWT
jwt.secret=VotreCleSecreteTresLongueEtSecurisee123456789
jwt.expiration=86400000

# Port du serveur
server.port=8080

# Configuration CORS (pour le frontend)
cors.allowed-origins=http://localhost:3000,http://localhost:4200

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.com.appAuditza=DEBUG